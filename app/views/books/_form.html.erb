<div class="card col-sm-6 mx-auto mt-5">
  <% if book.errors.any? %>	  <div class="card-body">
    <div id="error_explanation">	    <h3 class="mt-1 mb-3">新しい本の追加</h3>
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>	    <%= form_with(model: book, local: true) do |form| %>

    <% if book.errors.any? %>
      <ul>	      <div id="error_explanation">　
        <% book.errors.full_messages.each do |message| %>	        <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>
          <li><%= message %></li>	        <ul>
        <% end %>	          <% book.errors.full_messages.each do |message| %>
      </ul>	            <li><%= message %></li>
    </div>	          <% end %>
  <% end %>	        </ul>

      </div>
  <div class="field">	    <% end %>
    <%= form.label :title %>	    <div class="field">
    <%= form.text_field :title %>	      <%= form.label :title, "本のタイトル", :class => "form-label" %>
  </div>	      <%= form.text_field :title, :class => "form-control" %>
    </div>


  <div class="field">	    <div class="field">
    <%= form.label :number_of_pages %>	      <%= form.label :number_of_pages, "ページ数", :class => "form-label" %>
    <%= form.number_field :number_of_pages %>	      <%= form.number_field :number_of_pages, :class => "form-control" %>
  </div>	    </div>

  <div class="field">	
    <%= form.label :detail %>	
    <%= form.text_area :detail %>	
  </div>	


  <div class="field">	    <div class="field">
    <%= form.label :image %>	      <%= form.label :detail, "本の内容", :class => "form-label"  %>
    <%= form.file_field :image %><br>	      <%= form.text_area :detail, :class => "form-control"%>
    </div>

    <div class="field">
      <%= form.label :image, "本"  %>
      <%= form.file_field :image %><br>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
    <% end %>
  </div>	  </div>

</div><div class="card col-sm-6 mx-auto mt-5">
  <div class="actions">	  <div class="card-body">
    <%= form.submit %>	    <h3 class="mt-1 mb-3">新しい本の追加</h3>
    <%= form_with(model: book, local: true) do |form| %>
    <% if book.errors.any? %>
      <div id="error_explanation">　
        <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>
        <ul>
          <% book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= form.label :title, "本のタイトル", :class => "form-label" %>
      <%= form.text_field :title, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :number_of_pages, "ページ数", :class => "form-label" %>
      <%= form.number_field :number_of_pages, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :detail, "本の内容", :class => "form-label"  %>
      <%= form.text_area :detail, :class => "form-control"%>
    </div>

    <div class="field">
      <%= form.label :image, "本"  %>
      <%= form.file_field :image %><br>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
    <% end %>
  </div>	  </div>
<% end %>	</div>